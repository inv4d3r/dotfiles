auto-reload no
browser qutebrowser
color background white black
color info black white
color listnormal blue black
color listnormal_unread blue black
color listfocus white blue
color listfocus_unread black blue

unbind-key h
unbind-key j
unbind-key k
unbind-key l

bind-key h quit
bind-key j down
bind-key k up
bind-key l open

unbind-key g # bound to `sort` by default
bind-key g home
unbind-key G # bound to `rev-sort` by default
bind-key G end

unbind-key ^F # bound to 'clear-filter' by default
bind-key ^F pagedown
unbind-key ^B # bound to 'bookmark' by default
bind-key ^B pageup
